services:
  - type: web
    name: veritas-foresight
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn foresight_app:app --bind 0.0.0.0:$PORT --workers 1 --timeout 60
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
